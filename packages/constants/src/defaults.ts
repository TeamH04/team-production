import type { MoneyBucket, ShopCategory } from '@team/types';

/**
 * 店舗データのデフォルト値
 */
export const DEFAULT_SHOP_CATEGORY: ShopCategory = 'カフェ・喫茶';
export const DEFAULT_SHOP_BUDGET: MoneyBucket = '$$';
export const DEFAULT_DISTANCE_MINUTES = 5;
export const DEFAULT_RATING = 0;
